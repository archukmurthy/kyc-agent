node_modules
build
.env
.env.local
.vercel
.DS_Store

.vercel
.config-store.json
.claude/

# Benchmark scratch output + local DB migration (applied directly to Neon)
*-benchmark.json
benchmark/out/
migrate.sql

# Doc search agent downloads (audit copies of fetched PDFs — local dev only)
downloads/

# Self-source agent test-run output (generated locally — not source)
test-output-self-source/

# Local notification helper (run by Claude Code after task completion)
alert.ps1

# Local idle-watcher helper (run in a separate PowerShell window)
watch-claude.ps1
