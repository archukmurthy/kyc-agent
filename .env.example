# Copy this file to .env.local and fill in real values for local dev.
# All four variables must also be set in the Vercel project settings for production.

# Anthropic API key — used by api/research.js to proxy KYC research requests.
ANTHROPIC_API_KEY=sk-ant-...

# Admin password — gates the /admin configuration UI.
ADMIN_PASSWORD=nium-admin-2026

# Tenant ID — keys this deployment's config in storage. Change per tenant when
# multi-tenant. Config is stored under config:{TENANT_ID}.
TENANT_ID=nium

# Optional: when @vercel/kv is provisioned the config API uses it automatically.
# Without these env vars the config API falls back to filesystem storage
# (./.config-store.json in dev, /tmp/config-store.json on Vercel).
# KV_REST_API_URL=https://...
# KV_REST_API_TOKEN=...
