# UBO demo backlog

## Registry-context consolidation

Deferred. Consolidate repeated registry observations into one entity context with expandable source observations, grouped only by exact registry identity. Preserve each observation's provenance, differences and padded source identifiers. Do not treat repeated registry observations as independent-source corroboration.

This item is presentation-only backlog scope. It is not implemented by the director-control label correction.
